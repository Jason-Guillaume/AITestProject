[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
markers =
    smoke: 核心冒烟用例（关键链路）
    regression: 回归用例（覆盖主要业务流程）
    full: 全量用例（完整接口矩阵）
