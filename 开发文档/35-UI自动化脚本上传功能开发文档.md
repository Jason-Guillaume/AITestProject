# UI自动化脚本上传功能开发文档

## 文档信息
- **创建日期**: 2026-04-30
- **模块名称**: UI自动化脚本上传
- **开发阶段**: 阶段1 - Django模型与上传接口
- **负责人**: 全栈开发团队

---

## 一、功能概述

### 1.1 业务背景
为支持UI自动化测试能力，需要提供脚本上传功能，支持两种主流自动化框架：
- **LINEAR模式**: 线性脚本，单文件执行
- **POM模式**: Page Object Model，支持多文件项目结构

### 1.2 核心功能
1. 支持单文件上传（.py文件）
2. 支持ZIP包上传（包含多个文件的项目）
3. 支持Git仓库URL关联
4. 自动解压ZIP到独立工作空间
5. 记录脚本元数据（名称、类型、入口点等）

---

## 二、数据库设计

### 2.1 模型定义

**文件路径**: `assistant/models.py`

```python
from django.db import models
from django.core.validators import FileExtensionValidator
from django.utils.translation import gettext_lazy as _
import os


class UIScriptUpload(models.Model):
    """UI自动化脚本上传模型"""
    
    class ScriptType(models.TextChoices):
        LINEAR = 'LINEAR', _('线性脚本')
        POM = 'POM', _('Page Object Model')
    
    # 基础字段
    id = models.AutoField(primary_key=True, verbose_name='脚本ID')
    name = models.CharField(
        max_length=255, 
        verbose_name='脚本名称',
        help_text='脚本的显示名称'
    )
    script_type = models.CharField(
        max_length=10,
        choices=ScriptType.choices,
        default=ScriptType.LINEAR,
        verbose_name='脚本类型'
    )
    
    # 文件存储
    file_path = models.FileField(
        upload_to='ui_scripts/%Y/%m/%d/',
        validators=[FileExtensionValidator(allowed_extensions=['py', 'zip'])],
        verbose_name='脚本文件',
        help_text='单文件(.py)或ZIP包'
    )
    
    # Git仓库（可选）
    git_repo_url = models.URLField(
        max_length=500,
        blank=True,
        null=True,
        verbose_name='Git仓库URL',
        help_text='关联的Git仓库地址（可选）'
    )
    
    # 执行入口
    entry_point = models.CharField(
        max_length=500,
        verbose_name='执行入口路径',
        help_text='脚本执行的入口文件路径，如: main.py 或 tests/test_login.py'
    )
    
    # 工作空间路径（解压后的目录）
    workspace_path = models.CharField(
        max_length=500,
        blank=True,
        null=True,
        verbose_name='工作空间路径',
        help_text='ZIP解压后的目录路径'
    )
    
    # 时间戳
    created_at = models.DateTimeField(
        auto_now_add=True,
        verbose_name='创建时间'
    )
    updated_at = models.DateTimeField(
        auto_now=True,
        verbose_name='更新时间'
    )
    
    # 状态字段
    is_active = models.BooleanField(
        default=True,
        verbose_name='是否启用'
    )
    
    class Meta:
        db_table = 'ui_script_upload'
        verbose_name = 'UI自动化脚本'
        verbose_name_plural = 'UI自动化脚本'
        ordering = ['-created_at']
        indexes = [
            models.Index(fields=['script_type']),
            models.Index(fields=['created_at']),
        ]
    
    def __str__(self):
        return f"{self.name} ({self.get_script_type_display()})"
    
    @property
    def file_extension(self):
        """获取文件扩展名"""
        return os.path.splitext(self.file_path.name)[1]
    
    @property
    def is_zip(self):
        """判断是否为ZIP文件"""
        return self.file_extension.lower() == '.zip'
```

### 2.2 数据库迁移

```bash
# 生成迁移文件
python manage.py makemigrations assistant

# 执行迁移
python manage.py migrate assistant
```

---

## 三、序列化器设计

**文件路径**: `assistant/serializers.py`

```python
from rest_framework import serializers
from .models import UIScriptUpload
import os


class UIScriptUploadSerializer(serializers.ModelSerializer):
    """UI脚本上传序列化器"""
    
    file_extension = serializers.ReadOnlyField()
    is_zip = serializers.ReadOnlyField()
    file_size = serializers.SerializerMethodField()
    
    class Meta:
        model = UIScriptUpload
        fields = [
            'id',
            'name',
            'script_type',
            'file_path',
            'git_repo_url',
            'entry_point',
            'workspace_path',
            'created_at',
            'updated_at',
            'is_active',
            'file_extension',
            'is_zip',
            'file_size',
        ]
        read_only_fields = ['id', 'workspace_path', 'created_at', 'updated_at']
    
    def get_file_size(self, obj):
        """获取文件大小（字节）"""
        try:
            return obj.file_path.size
        except Exception:
            return None
    
    def validate_file_path(self, value):
        """验证文件类型和大小"""
        # 文件大小限制：50MB
        max_size = 50 * 1024 * 1024
        if value.size > max_size:
            raise serializers.ValidationError(
                f"文件大小不能超过50MB，当前大小: {value.size / 1024 / 1024:.2f}MB"
            )
        
        # 验证文件扩展名
        ext = os.path.splitext(value.name)[1].lower()
        if ext not in ['.py', '.zip']:
            raise serializers.ValidationError(
                f"不支持的文件类型: {ext}，仅支持 .py 和 .zip"
            )
        
        return value
    
    def validate(self, attrs):
        """交叉字段验证"""
        script_type = attrs.get('script_type')
        file_path = attrs.get('file_path')
        
        if file_path:
            ext = os.path.splitext(file_path.name)[1].lower()
            
            # LINEAR模式建议使用单文件
            if script_type == UIScriptUpload.ScriptType.LINEAR and ext == '.zip':
                # 警告但不阻止
                pass
            
            # POM模式建议使用ZIP包
            if script_type == UIScriptUpload.ScriptType.POM and ext == '.py':
                # 警告但不阻止
                pass
        
        return attrs


class UIScriptUploadListSerializer(serializers.ModelSerializer):
    """列表展示序列化器（精简版）"""
    
    file_size = serializers.SerializerMethodField()
    
    class Meta:
        model = UIScriptUpload
        fields = [
            'id',
            'name',
            'script_type',
            'entry_point',
            'created_at',
            'is_active',
            'file_size',
        ]
    
    def get_file_size(self, obj):
        """获取文件大小（格式化）"""
        try:
            size = obj.file_path.size
            if size < 1024:
                return f"{size}B"
            elif size < 1024 * 1024:
                return f"{size / 1024:.2f}KB"
            else:
                return f"{size / 1024 / 1024:.2f}MB"
        except Exception:
            return "未知"
```

---

## 四、视图集设计

**文件路径**: `assistant/views.py`

```python
from rest_framework import viewsets, status
from rest_framework.decorators import action
from rest_framework.response import Response
from rest_framework.parsers import MultiPartParser, FormParser
from django.db import transaction
from .models import UIScriptUpload
from .serializers import UIScriptUploadSerializer, UIScriptUploadListSerializer
from .utils.script_handler import handle_script_upload
import logging

logger = logging.getLogger(__name__)


class UIScriptUploadViewSet(viewsets.ModelViewSet):
    """UI自动化脚本上传视图集"""
    
    queryset = UIScriptUpload.objects.all()
    parser_classes = [MultiPartParser, FormParser]
    
    def get_serializer_class(self):
        """根据action选择序列化器"""
        if self.action == 'list':
            return UIScriptUploadListSerializer
        return UIScriptUploadSerializer
    
    @transaction.atomic
    def create(self, request, *args, **kwargs):
        """
        创建脚本上传记录
        
        请求参数:
        - name: 脚本名称
        - script_type: LINEAR 或 POM
        - file_path: 文件（.py 或 .zip）
        - git_repo_url: Git仓库URL（可选）
        - entry_point: 执行入口路径
        """
        serializer = self.get_serializer(data=request.data)
        serializer.is_valid(raise_exception=True)
        
        # 保存模型实例
        instance = serializer.save()
        
        try:
            # 处理文件上传（解压ZIP等）
            handle_script_upload(instance)
            
            logger.info(f"脚本上传成功: {instance.name} (ID: {instance.id})")
            
            return Response(
                UIScriptUploadSerializer(instance).data,
                status=status.HTTP_201_CREATED
            )
        
        except Exception as e:
            logger.error(f"脚本上传处理失败: {str(e)}", exc_info=True)
            # 回滚事务
            instance.delete()
            return Response(
                {'error': f'文件处理失败: {str(e)}'},
                status=status.HTTP_500_INTERNAL_SERVER_ERROR
            )
    
    @action(detail=True, methods=['post'])
    def toggle_active(self, request, pk=None):
        """切换脚本启用状态"""
        instance = self.get_object()
        instance.is_active = not instance.is_active
        instance.save()
        
        return Response({
            'id': instance.id,
            'is_active': instance.is_active,
            'message': f"脚本已{'启用' if instance.is_active else '禁用'}"
        })
    
    @action(detail=True, methods=['get'])
    def workspace_info(self, request, pk=None):
        """获取工作空间信息"""
        instance = self.get_object()
        
        if not instance.workspace_path:
            return Response(
                {'error': '该脚本没有工作空间'},
                status=status.HTTP_404_NOT_FOUND
            )
        
        import os
        workspace_path = instance.workspace_path
        
        if not os.path.exists(workspace_path):
            return Response(
                {'error': '工作空间目录不存在'},
                status=status.HTTP_404_NOT_FOUND
            )
        
        # 列出工作空间文件
        files = []
        for root, dirs, filenames in os.walk(workspace_path):
            for filename in filenames:
                rel_path = os.path.relpath(
                    os.path.join(root, filename),
                    workspace_path
                )
                files.append(rel_path)
        
        return Response({
            'workspace_path': workspace_path,
            'files': files,
            'file_count': len(files)
        })
```

---

## 五、文件处理工具

**文件路径**: `assistant/utils/script_handler.py`

```python
import os
import zipfile
import shutil
from django.conf import settings
import logging

logger = logging.getLogger(__name__)


def handle_script_upload(instance):
    """
    处理脚本上传后的操作
    
    Args:
        instance: UIScriptUpload模型实例
    
    功能:
    1. 如果是ZIP文件，解压到工作空间
    2. 验证入口点文件是否存在
    3. 更新workspace_path字段
    """
    file_path = instance.file_path.path
    
    # 如果是ZIP文件，需要解压
    if instance.is_zip:
        workspace_path = _extract_zip(instance, file_path)
        instance.workspace_path = workspace_path
        instance.save(update_fields=['workspace_path'])
        
        logger.info(f"ZIP文件已解压到: {workspace_path}")
    else:
        # 单文件，工作空间就是文件所在目录
        instance.workspace_path = os.path.dirname(file_path)
        instance.save(update_fields=['workspace_path'])
    
    # 验证入口点
    _validate_entry_point(instance)


def _extract_zip(instance, zip_path):
    """
    解压ZIP文件到工作空间
    
    Args:
        instance: UIScriptUpload实例
        zip_path: ZIP文件路径
    
    Returns:
        str: 解压后的工作空间路径
    """
    # 创建工作空间目录: media/workspaces/{id}/
    workspace_base = os.path.join(settings.MEDIA_ROOT, 'workspaces')
    os.makedirs(workspace_base, exist_ok=True)
    
    workspace_path = os.path.join(workspace_base, str(instance.id))
    
    # 如果目录已存在，先删除
    if os.path.exists(workspace_path):
        shutil.rmtree(workspace_path)
    
    os.makedirs(workspace_path)
    
    # 解压ZIP
    try:
        with zipfile.ZipFile(zip_path, 'r') as zip_ref:
            zip_ref.extractall(workspace_path)
        
        logger.info(f"ZIP解压成功: {zip_path} -> {workspace_path}")
        return workspace_path
    
    except zipfile.BadZipFile as e:
        logger.error(f"ZIP文件损坏: {zip_path}")
        raise ValueError(f"ZIP文件损坏: {str(e)}")
    
    except Exception as e:
        logger.error(f"ZIP解压失败: {str(e)}", exc_info=True)
        raise


def _validate_entry_point(instance):
    """
    验证入口点文件是否存在
    
    Args:
        instance: UIScriptUpload实例
    
    Raises:
        FileNotFoundError: 入口点文件不存在
    """
    workspace_path = instance.workspace_path
    entry_point = instance.entry_point
    
    # 构建完整路径
    entry_full_path = os.path.join(workspace_path, entry_point)
    
    if not os.path.exists(entry_full_path):
        error_msg = f"入口点文件不存在: {entry_point}"
        logger.error(error_msg)
        raise FileNotFoundError(error_msg)
    
    if not entry_full_path.endswith('.py'):
        error_msg = f"入口点必须是Python文件: {entry_point}"
        logger.error(error_msg)
        raise ValueError(error_msg)
    
    logger.info(f"入口点验证通过: {entry_full_path}")


def cleanup_workspace(instance):
    """
    清理工作空间（删除脚本时调用）
    
    Args:
        instance: UIScriptUpload实例
    """
    if instance.workspace_path and os.path.exists(instance.workspace_path):
        try:
            # 只删除workspaces下的目录
            if 'workspaces' in instance.workspace_path:
                shutil.rmtree(instance.workspace_path)
                logger.info(f"工作空间已清理: {instance.workspace_path}")
        except Exception as e:
            logger.error(f"清理工作空间失败: {str(e)}", exc_info=True)
```

---

## 六、Django信号处理

**文件路径**: `assistant/signals.py`

```python
from django.db.models.signals import post_save, pre_delete
from django.dispatch import receiver
from .models import UIScriptUpload
from .utils.script_handler import handle_script_upload, cleanup_workspace
import logging

logger = logging.getLogger(__name__)


@receiver(post_save, sender=UIScriptUpload)
def handle_ui_script_post_save(sender, instance, created, **kwargs):
    """
    脚本保存后的信号处理
    
    注意: 由于文件处理已在ViewSet中完成，这里仅做日志记录
    """
    if created:
        logger.info(f"新脚本已创建: {instance.name} (ID: {instance.id})")


@receiver(pre_delete, sender=UIScriptUpload)
def handle_ui_script_pre_delete(sender, instance, **kwargs):
    """
    脚本删除前的信号处理 - 清理工作空间
    """
    logger.info(f"准备删除脚本: {instance.name} (ID: {instance.id})")
    cleanup_workspace(instance)
```

**注册信号** - 在 `assistant/apps.py`:

```python
from django.apps import AppConfig


class AssistantConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'assistant'
    
    def ready(self):
        import assistant.signals  # 导入信号
```

---

## 七、URL路由配置

**文件路径**: `assistant/urls.py`

```python
from django.urls import path, include
from rest_framework.routers import DefaultRouter
from .views import UIScriptUploadViewSet

router = DefaultRouter()
router.register(r'ui-scripts', UIScriptUploadViewSet, basename='ui-script')

urlpatterns = [
    path('', include(router.urls)),
]
```

**主路由配置** - 在 `AITestProduct/urls.py`:

```python
from django.urls import path, include

urlpatterns = [
    # ... 其他路由
    path('api/assistant/', include('assistant.urls')),
]
```

---

## 八、API接口文档

### 8.1 创建脚本上传

**接口**: `POST /api/assistant/ui-scripts/`

**请求头**:
```
Content-Type: multipart/form-data
```

**请求参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | string | 是 | 脚本名称 |
| script_type | string | 是 | LINEAR 或 POM |
| file_path | file | 是 | .py 或 .zip 文件 |
| entry_point | string | 是 | 执行入口路径 |
| git_repo_url | string | 否 | Git仓库URL |

**响应示例**:
```json
{
  "id": 1,
  "name": "登录测试脚本",
  "script_type": "LINEAR",
  "file_path": "/media/ui_scripts/2026/04/30/test_login.py",
  "git_repo_url": null,
  "entry_point": "test_login.py",
  "workspace_path": "/media/ui_scripts/2026/04/30",
  "created_at": "2026-04-30T10:00:00Z",
  "updated_at": "2026-04-30T10:00:00Z",
  "is_active": true,
  "file_extension": ".py",
  "is_zip": false,
  "file_size": 2048
}
```

### 8.2 获取脚本列表

**接口**: `GET /api/assistant/ui-scripts/`

**响应示例**:
```json
[
  {
    "id": 1,
    "name": "登录测试脚本",
    "script_type": "LINEAR",
    "entry_point": "test_login.py",
    "created_at": "2026-04-30T10:00:00Z",
    "is_active": true,
    "file_size": "2.00KB"
  }
]
```

### 8.3 获取脚本详情

**接口**: `GET /api/assistant/ui-scripts/{id}/`

### 8.4 切换启用状态

**接口**: `POST /api/assistant/ui-scripts/{id}/toggle_active/`

**响应示例**:
```json
{
  "id": 1,
  "is_active": false,
  "message": "脚本已禁用"
}
```

### 8.5 获取工作空间信息

**接口**: `GET /api/assistant/ui-scripts/{id}/workspace_info/`

**响应示例**:
```json
{
  "workspace_path": "/media/workspaces/1",
  "files": [
    "main.py",
    "pages/__init__.py",
    "pages/login_page.py",
    "tests/test_login.py"
  ],
  "file_count": 4
}
```

---

## 九、测试用例

**文件路径**: `assistant/tests/test_ui_script_upload.py`

```python
from django.test import TestCase
from django.core.files.uploadedfile import SimpleUploadedFile
from rest_framework.test import APIClient
from rest_framework import status
from assistant.models import UIScriptUpload
import os


class UIScriptUploadTestCase(TestCase):
    """UI脚本上传测试"""
    
    def setUp(self):
        self.client = APIClient()
        self.url = '/api/assistant/ui-scripts/'
    
    def test_upload_single_py_file(self):
        """测试上传单个Python文件"""
        file_content = b'print("Hello World")'
        uploaded_file = SimpleUploadedFile(
            "test_script.py",
            file_content,
            content_type="text/x-python"
        )
        
        data = {
            'name': '测试脚本',
            'script_type': 'LINEAR',
            'file_path': uploaded_file,
            'entry_point': 'test_script.py'
        }
        
        response = self.client.post(self.url, data, format='multipart')
        
        self.assertEqual(response.status_code, status.HTTP_201_CREATED)
        self.assertEqual(UIScriptUpload.objects.count(), 1)
        
        script = UIScriptUpload.objects.first()
        self.assertEqual(script.name, '测试脚本')
        self.assertEqual(script.script_type, 'LINEAR')
        self.assertFalse(script.is_zip)
    
    def test_file_size_validation(self):
        """测试文件大小验证"""
        # 创建超过50MB的文件
        large_content = b'x' * (51 * 1024 * 1024)
        uploaded_file = SimpleUploadedFile(
            "large_script.py",
            large_content,
            content_type="text/x-python"
        )
        
        data = {
            'name': '大文件脚本',
            'script_type': 'LINEAR',
            'file_path': uploaded_file,
            'entry_point': 'large_script.py'
        }
        
        response = self.client.post(self.url, data, format='multipart')
        
        self.assertEqual(response.status_code, status.HTTP_400_BAD_REQUEST)
        self.assertIn('文件大小不能超过50MB', str(response.data))
```

---

## 十、部署清单

### 10.1 数据库迁移
```bash
python manage.py makemigrations assistant
python manage.py migrate assistant
```

### 10.2 创建媒体目录
```bash
mkdir -p media/ui_scripts
mkdir -p media/workspaces
chmod 755 media/ui_scripts
chmod 755 media/workspaces
```

### 10.3 配置文件检查

**settings.py**:
```python
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

# 文件上传配置
FILE_UPLOAD_MAX_MEMORY_SIZE = 52428800  # 50MB
DATA_UPLOAD_MAX_MEMORY_SIZE = 52428800  # 50MB
```

### 10.4 依赖检查
```bash
pip install djangorestframework
pip install pillow  # 如果需要处理图片
```

---

## 十一、后续开发计划

### 阶段2：执行引擎集成
- Selenium/Playwright执行器
- 执行日志收集
- 截图/录屏功能

### 阶段3：前端界面
- 脚本上传表单
- 脚本列表展示
- 执行结果查看

### 阶段4：高级功能
- 脚本版本管理
- 定时执行
- 并发执行控制

---

## 十二、注意事项

1. **安全性**:
   - 上传的脚本需要进行安全扫描
   - 限制可执行的Python模块
   - 隔离执行环境

2. **性能**:
   - ZIP解压使用异步任务（Celery）
   - 大文件上传使用分片上传
   - 工作空间定期清理

3. **兼容性**:
   - 支持Python 3.8+
   - 兼容Windows/Linux路径
   - 处理中文文件名

---

## 文档变更记录

| 日期 | 版本 | 变更内容 | 作者 |
|------|------|----------|------|
| 2026-04-30 | 1.0 | 初始版本，完成阶段1设计 | 开发团队 |
