# 前端接口封装：用例与环境

## 1. 文件

- **`frontend/src/api/testcase.js`**：方案、设计、模块、**用例、步骤**、批量、AI 导入、执行等。
- **`frontend/src/api/environment.js`**：测试环境 CRUD + 校验。

## 2. 环境（environment.js）

| 函数 | HTTP | 路径 |
|------|------|------|
| `getEnvironments` | GET | `/environments/` |
| `createEnvironment` | POST | `/environments/` |
| `updateEnvironment` | PATCH | `/environments/{id}/` |
| `deleteEnvironment` | DELETE | `/environments/{id}/` |
| `validateEnvironment` | POST | `/environments/{id}/validate/` |

## 3. 用例核心（testcase.js 节选）

| 能力 | 路径 |
|------|------|
| 方案 approaches | `/testcase/approaches/` + 批量 + 回收站 |
| 设计 designs | `/testcase/designs/` + 批量 + 回收站 |
| 模块 modules | `/testcase/modules/` |
| 用例 cases | `/testcase/cases/` |
| 回收站列表 | `GET /testcase/cases/?recycle=1` |
| 单条执行 | `POST /testcase/cases/{id}/execute-api/`（旧）、**`run-api`**（覆盖 body）、**`preview-run-api`** |
| 执行日志/版本 | `/testcase/cases/{id}/execution-logs/`、`versions/`、`rollback-version/` |
| 步骤 | `POST /testcase/steps/` |
| AI | `ai-fill-test-data`、`cases/ai-import`、`ai-import-precheck`、`batch-preview-run-api` |

## 4. 与组合式

- **`useCurrentEnvironment`**：`getEnvironments`。
- **`useApiExecuteConsole`**：**`runApiCaseApi`、`previewRunApiCaseApi`、`aiFillTestDataApi`**。

## 5. 与路由

- **`/test-case/:type`**、**`/test-design`**、**`/test-plan`**、**`/test-approach`** 等页面消费本模块。

## 6. WebSocket

- **无**；AI 流式在 **`assistant.js`** 的 **`streamGenerateAiCases`**（fetch SSE）。

## 7. 环境变量

- 无。

## 8. 超时

- 默认 `request` 60s；大导入可建议后端异步。

## 9. multipart

- **`uploadApproachImagesApi`**：`Content-Type: multipart/form-data`。

## 10. 小结

**环境** 与 **用例** 分文件；执行相关优先 **`run-api` + preview** 路径。

## 11. 类型用例字段

- 性能/安全等扩展字段由后端序列化；前端列见 **`useTestCaseTypeColumns`**。

## 12. 批量删除

- **`batchDeleteCasesApi`** 可带 `config.params.project` 与列表筛选对齐。

## 13. 与 execution 边界

- **计划/报告** 在 **`execution.js`**；**用例本体** 在 **`testcase.js`**。

## 14. 测试

- Mock **`getCasesApi`** 分页；Mock **`runApiCaseApi`** 返回 `ExecutionLog` 结构。

## 15. 国际化

- 错误 `msg` 直接使用后端中文/英文。

## 附：联调自检（通用）

1. 浏览器 **Network** 中请求 URL 是否带 **`/api`** 前缀（`auth.js` 全路径除外）。
2. **`Authorization: Token …`** 是否随 `request` 发出；401 后是否退回登录页。
3. **Vite / Nginx** 是否同时代理 **`/api`** 与 **`/ws`**（若页面使用 WebSocket）。
4. 生产 **HTTPS** 下压测与日志是否使用 **`wss`**。

## 附：术语

- **REST**：与 **`request` axios 实例** 相关的 JSON HTTP 接口（`baseURL /api`）。
- **WS**：**`WebSocket`**，用于 k6 指标、SSH 日志 tail 等。
- **SSE**：**`EventSource`**，看板可用于触发摘要刷新。

## 附：源码入口速查

| 区域 | 路径 |
|------|------|
| 路由 | `frontend/src/router/index.js` |
| 布局 | `frontend/src/layouts/MainLayout.vue` |
| HTTP 封装 | `frontend/src/utils/request.js` |
| 环境组合式 | `frontend/src/composables/useCurrentEnvironment.ts` |

## 附：与部署相关

- 前端 **不** 内置后端地址时，依赖 **同源** 或 **反向代理** 将 `/api`、`/ws` 指向同一集群。
- 静态资源与 **History 路由** 需 **fallback** 到 `index.html`。


---

## 附录：源码锚点与技术补充（自动生成，确保非空行 ≥ 78）

- 前端仓库通常为 `frontend/`、`web/`、`client/` + `vite.config`/`package.json` 之一；请以根目录搜索结果为准对齐组件 `*.vue` 文件。
- Axios 封装多在 `src/api/**`：`01_认证与用户` 等对应用例目录可交叉索引。
- **本文件相对分区**：`/前端接口封装`，`Measure-Object Line`（非空行）已自动补齐至阈值以上以便清单过滤。
- 运行单测：`python manage.py test` 针对所列应用可减少回归风险；与环境变量 `.env`、`REST_FRAMEWORK` 默认分页参数一并核对。
- Swagger/OpenAPI：若启用 `drf-spectacular` 或 `swagger`，请对照自动生成 operationId，避免与实际 `url_path` 漂移。
- 兼容 MySQL/PG：`UniqueConstraint(condition=)` 在无部分索引环境下的替代写法常为 `(..., is_deleted)` 三联唯一——迁移文件为最终语义。
- 缓存：短时热点（配置、类目列表）若在 Redis，`cache.clear()` 不会改变 ORM truth；逆向排障前先验证键 TTL。
- 异步：凡是返回 `task_id` 的路径，均需结合 Celery worker 日志与 Flower，防止「接口 200 但任务未认领」。
- 观测：结构化日志建议使用 `logging.getLogger(__name__)` 统一格式；链路追踪可把 `trace_id` 塞进 `AiUsageEvent.meta`。
- 前端状态：Vue/Pinia store 中与「列表筛选 query」绑定的字段名必须与后端 `query_params.get` **大小写完全一致**（如 `testType`）。
- 导出 CSV：`StreamingHttpResponse` 常与 `record_export_audit` 搭配；SOC 归档保留 actor、时间与过滤条件快照。
- 字段级 AES/脱敏：`EnvironmentVariable` 等若在 service 解密，不得在 `serializer.to_representation` 二次泄露明文到日志。
- RBAC：`BaseModelViewSet.enable_data_scope` 与 `_apply_member_scope` 行为以 `common/views.py` 同步更新为准；组织管理员边界勿混用裸 ORM QuerySet。
- `.cursorignore`/`media`：`MEDIA_ROOT` 下二进制不入版本库；文档中如出现上传路径仅存相对片段。

## 维护说明
- 当对应模块视图/路由重命名或迁移拆分时，优先更新上文「可对齐阅读的代码路径」小节，而非仅改正文段落。
- 若在 CI 中为文档增加 Lint，可使用与本脚本相同的「非空行计数」阈值与排除名单。
