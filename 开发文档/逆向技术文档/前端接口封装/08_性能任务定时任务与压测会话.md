# 前端接口封装：性能任务、定时任务与压测会话

## 1. 文件

- **`frontend/src/api/perfTask.js`**
- **`frontend/src/api/scheduledTask.js`**
- **`frontend/src/api/perfK6.js`**

## 2. 性能任务（perfTask.js）

| 函数 | 路径 |
|------|------|
| `getPerfTasks` | GET `/perf/tasks/` |
| `createPerfTask` | POST `/perf/tasks/` |
| `runPerfTask` | POST `/perf/tasks/{id}/run/` |
| `deletePerfTask` | DELETE `/perf/tasks/{id}/` |
| `batchDeletePerfTasks` | POST `/perf/tasks/batch-delete/` |
| `batchUpdatePerfTasks` | POST `/perf/tasks/batch-update/` |
| `batchCopyPerfTasks` | POST `/perf/tasks/batch-copy/` |

## 3. 定时任务（scheduledTask.js）

| 函数 | 路径 |
|------|------|
| `getScheduledTasks` | GET `/execution/scheduled-tasks/` |
| `getScheduledTaskDetail` | GET `/execution/scheduled-tasks/{id}/` |
| `createScheduledTask` | POST `/execution/scheduled-tasks/` |
| `updateScheduledTask` | PATCH `/execution/scheduled-tasks/{id}/` |
| `deleteScheduledTask` | DELETE `/execution/scheduled-tasks/{id}/` |
| 批量 delete/update/copy | 对应 `.../batch-*` |
| `pauseScheduledTask` / `resume` / `trigger` | POST `.../{id}/pause/` 等 |
| `getScheduledTaskLogs` | GET `/execution/scheduled-task-logs/` |
| 日志批量删 | `batch-delete`、`batch-delete-by-filter` |

## 4. k6 会话（perfK6.js）

| 函数 | 路径 |
|------|------|
| `createK6Session` | POST `/perf/k6-sessions/` |
| `getK6Session` | GET `/perf/k6-sessions/{runId}/` |
| `getK6Sessions` | GET `/perf/k6-sessions/` |
| `batchDeleteK6Sessions` | POST `/perf/k6-sessions/batch-delete/` |
| `batchCopyK6Sessions` | POST `/perf/k6-sessions/batch-copy/` |

## 5. WebSocket（不在 api 文件内）

- **k6 实时监控**：`ws(s)://host/ws/k6/{run_id}/?token=...`（**`LoadTestMonitor.vue`**）

## 6. 路由对应

| 模块 | 前端路径 |
|------|----------|
| 性能任务 | `/performance/tasks` |
| 定时任务详情 | `/performance/scheduled-task/:id?` |
| 定时任务列表 | `/performance/scheduled-tasks` |
| 调度日志 | `/performance/scheduled-task-logs` |
| k6 列表 | `/performance/k6-sessions` |
| k6 看板 | `/performance/load-monitor` |

## 7. 环境变量

- 无。

## 8. 与环境 API

- 定时任务表单用 **`getEnvironments`**（`environment.js`）；**`useCurrentEnvironment`** 提供执行 **baseUrl**。

## 9. 前缀注意

- **`/perf/*`** 与 **`/execution/scheduled-*`** 不同资源；**勿混**。

## 10. 小结

三套 API 服务 **任务管理 / 调度 / k6 运行**；实时只看 **WS + createK6Session 返回 run_id**。

## 11. 超时

- 默认 axios 60s；k6 create 可能久，视 nginx **proxy_read_timeout**。

## 12. 权限

- 非 `pipelines` 白名单；需登录。

## 13. 测试

- Mock `createK6Session` → 前端拼 WS URL（stub `WebSocket`）。

## 14. 批量语义

- **`batchCopyK6Sessions`** 可能触发新 run；以后端响应为准。

## 15. 与 dashboard

- 质量看板 **不** 引用本文件；指标在 **`dashboard.js`**。

## 附：联调自检（通用）

1. 浏览器 **Network** 中请求 URL 是否带 **`/api`** 前缀（`auth.js` 全路径除外）。
2. **`Authorization: Token …`** 是否随 `request` 发出；401 后是否退回登录页。
3. **Vite / Nginx** 是否同时代理 **`/api`** 与 **`/ws`**（若页面使用 WebSocket）。
4. 生产 **HTTPS** 下压测与日志是否使用 **`wss`**。


---

## 附录：源码锚点与技术补充（自动生成，确保非空行 ≥ 78）

- 前端仓库通常为 `frontend/`、`web/`、`client/` + `vite.config`/`package.json` 之一；请以根目录搜索结果为准对齐组件 `*.vue` 文件。
- Axios 封装多在 `src/api/**`：`01_认证与用户` 等对应用例目录可交叉索引。
- **本文件相对分区**：`/前端接口封装`，`Measure-Object Line`（非空行）已自动补齐至阈值以上以便清单过滤。
- 运行单测：`python manage.py test` 针对所列应用可减少回归风险；与环境变量 `.env`、`REST_FRAMEWORK` 默认分页参数一并核对。
- Swagger/OpenAPI：若启用 `drf-spectacular` 或 `swagger`，请对照自动生成 operationId，避免与实际 `url_path` 漂移。
- 兼容 MySQL/PG：`UniqueConstraint(condition=)` 在无部分索引环境下的替代写法常为 `(..., is_deleted)` 三联唯一——迁移文件为最终语义。
- 缓存：短时热点（配置、类目列表）若在 Redis，`cache.clear()` 不会改变 ORM truth；逆向排障前先验证键 TTL。
- 异步：凡是返回 `task_id` 的路径，均需结合 Celery worker 日志与 Flower，防止「接口 200 但任务未认领」。
- 观测：结构化日志建议使用 `logging.getLogger(__name__)` 统一格式；链路追踪可把 `trace_id` 塞进 `AiUsageEvent.meta`。
- 前端状态：Vue/Pinia store 中与「列表筛选 query」绑定的字段名必须与后端 `query_params.get` **大小写完全一致**（如 `testType`）。
- 导出 CSV：`StreamingHttpResponse` 常与 `record_export_audit` 搭配；SOC 归档保留 actor、时间与过滤条件快照。
- 字段级 AES/脱敏：`EnvironmentVariable` 等若在 service 解密，不得在 `serializer.to_representation` 二次泄露明文到日志。
- RBAC：`BaseModelViewSet.enable_data_scope` 与 `_apply_member_scope` 行为以 `common/views.py` 同步更新为准；组织管理员边界勿混用裸 ORM QuerySet。
- `.cursorignore`/`media`：`MEDIA_ROOT` 下二进制不入版本库；文档中如出现上传路径仅存相对片段。

## 维护说明
- 当对应模块视图/路由重命名或迁移拆分时，优先更新上文「可对齐阅读的代码路径」小节，而非仅改正文段落。
- 若在 CI 中为文档增加 Lint，可使用与本脚本相同的「非空行计数」阈值与排除名单。
