# 前端接口封装：系统智能配置、用量、配额与审计

## 1. 文件

- **`frontend/src/api/sysAiConfig.js`**
- **`frontend/src/api/sysAiUsage.js`**
- **`frontend/src/api/sysAiQuota.js`**
- **`frontend/src/api/system.js`**（组织、消息设置、角色、用户）
- **`frontend/src/api/audit.js`**

## 2. AI 模型配置（sysAiConfig.js）

| 函数 | 路径 |
|------|------|
| `getAiModelConfigApi` | GET `/sys/ai-config/` |
| `saveAiModelConfigApi` | PUT `/sys/ai-config/` |
| `disconnectAiModelConfigApi` | POST `/sys/ai-config/disconnect/` |
| `reconnectAiModelConfigApi` | POST `/sys/ai-config/reconnect/` |
| `deleteAiModelConfigApi` | DELETE `/sys/ai-config/` |

## 3. AI 用量（sysAiUsage.js）

| 函数 | 路径 |
|------|------|
| `getAiUsageEventsApi` | GET `/sys/ai-usage/events/` |
| `getAiUsageSummaryApi` | GET `/sys/ai-usage/summary/` |
| `getAiUsageMetricsApi` | GET `/sys/ai-usage/metrics/` |
| `getAiUsageTopErrorsApi` | GET `/sys/ai-usage/top-errors/` |
| `getAiUsageLatencyTrendApi` | GET `/sys/ai-usage/latency-trend/` |
| `downloadAiUsageExportCsv` | **`downloadAuthedGet('/api/sys/ai-usage/export.csv')`** |

## 4. AI 配额（sysAiQuota.js）

- **`/sys/ai-quota/policies/`** CRUD：list、create、get、update、delete。

## 5. 系统组织与消息（system.js）

- Orgs：`/user/orgs/`
- Message settings：`/user/message-settings/`
- Roles：`/user/roles/`
- Users：`/user/users/`

## 6. 审计（audit.js）

| 函数 | 路径 |
|------|------|
| `getSysAuditEventsApi` | GET `/sys/audit/events/` |
| `getMyAuditEventsApi` | GET `/user/me/audit/events/` |
| `downloadSysAuditExportCsv` | **`downloadAuthedGet('/api/sys/audit/export.csv')`** |

## 7. 路由（需系统管理员）

- `/system/ai-usage`、`/system/ai-quota`、`/system/audit`、`/system/org`、`/system/role`、`/system/user`、`/system/messages`
- 守卫 **`SYSTEM_ADMIN_ONLY_PREFIXES`**

## 8. WebSocket

- **无**。

## 9. 环境变量

- 无；CSV 下载必须 **Token Header**（`downloadAuthedGet`）。

## 10. 与 `ModelConfigForm`

- 直接消费 **`sysAiConfig`** API。

## 11. 小结

**sys/*** = 平台级 AI 运维；**system.js** = 租户组织与用户；**audit** = 系统/个人审计 + 导出。

## 12. 权限

- 非管理员访问前述前缀 → 重定向 **`/system/message`**（`router`）。

## 13. 分页

- events/metrics 等 `params` 透传。

## 14. 测试

- Mock `downloadAiUsageExportCsv` 触发 blob；勿 `window.open` 裸链。

## 15. 与 assistant 用量

- **sys/ai-usage** 为服务端统计；前端 AI 调用不重复计费逻辑。

## 附：联调自检（通用）

1. 浏览器 **Network** 中请求 URL 是否带 **`/api`** 前缀（`auth.js` 全路径除外）。
2. **`Authorization: Token …`** 是否随 `request` 发出；401 后是否退回登录页。
3. **Vite / Nginx** 是否同时代理 **`/api`** 与 **`/ws`**（若页面使用 WebSocket）。
4. 生产 **HTTPS** 下压测与日志是否使用 **`wss`**。

## 附：术语

- **REST**：与 **`request` axios 实例** 相关的 JSON HTTP 接口（`baseURL /api`）。
- **WS**：**`WebSocket`**，用于 k6 指标、SSH 日志 tail 等。
- **SSE**：**`EventSource`**，看板可用于触发摘要刷新。


---

## 附录：源码锚点与技术补充（自动生成，确保非空行 ≥ 78）

- 前端仓库通常为 `frontend/`、`web/`、`client/` + `vite.config`/`package.json` 之一；请以根目录搜索结果为准对齐组件 `*.vue` 文件。
- Axios 封装多在 `src/api/**`：`01_认证与用户` 等对应用例目录可交叉索引。
- **本文件相对分区**：`/前端接口封装`，`Measure-Object Line`（非空行）已自动补齐至阈值以上以便清单过滤。
- 运行单测：`python manage.py test` 针对所列应用可减少回归风险；与环境变量 `.env`、`REST_FRAMEWORK` 默认分页参数一并核对。
- Swagger/OpenAPI：若启用 `drf-spectacular` 或 `swagger`，请对照自动生成 operationId，避免与实际 `url_path` 漂移。
- 兼容 MySQL/PG：`UniqueConstraint(condition=)` 在无部分索引环境下的替代写法常为 `(..., is_deleted)` 三联唯一——迁移文件为最终语义。
- 缓存：短时热点（配置、类目列表）若在 Redis，`cache.clear()` 不会改变 ORM truth；逆向排障前先验证键 TTL。
- 异步：凡是返回 `task_id` 的路径，均需结合 Celery worker 日志与 Flower，防止「接口 200 但任务未认领」。
- 观测：结构化日志建议使用 `logging.getLogger(__name__)` 统一格式；链路追踪可把 `trace_id` 塞进 `AiUsageEvent.meta`。
- 前端状态：Vue/Pinia store 中与「列表筛选 query」绑定的字段名必须与后端 `query_params.get` **大小写完全一致**（如 `testType`）。
- 导出 CSV：`StreamingHttpResponse` 常与 `record_export_audit` 搭配；SOC 归档保留 actor、时间与过滤条件快照。
- 字段级 AES/脱敏：`EnvironmentVariable` 等若在 service 解密，不得在 `serializer.to_representation` 二次泄露明文到日志。
- RBAC：`BaseModelViewSet.enable_data_scope` 与 `_apply_member_scope` 行为以 `common/views.py` 同步更新为准；组织管理员边界勿混用裸 ORM QuerySet。
- `.cursorignore`/`media`：`MEDIA_ROOT` 下二进制不入版本库；文档中如出现上传路径仅存相对片段。

## 维护说明
- 当对应模块视图/路由重命名或迁移拆分时，优先更新上文「可对齐阅读的代码路径」小节，而非仅改正文段落。
- 若在 CI 中为文档增加 Lint，可使用与本脚本相同的「非空行计数」阈值与排除名单。
