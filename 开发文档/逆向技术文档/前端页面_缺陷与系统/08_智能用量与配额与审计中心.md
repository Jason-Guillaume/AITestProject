# 智能用量与配额与审计中心

以下 **`/system/ai-usage`、`/system/ai-quota`、`/system/audit`** 三条路由均在 **`SYSTEM_ADMIN_ONLY_PREFIXES`** 列表中：**非系统管理员**会被重定向到 **`/system/message`**。

## 1. AI 用量看板 `/system/ai-usage`

**视图**：`frontend/src/views/system/AiUsageDashboard.vue`。  

**API**（**`@/api/sysAiUsage.js`**）：见各 `getAiUsage*` 与 **`downloadAiUsageExportCsv`**（对应 **`GET /api/sys/ai-usage/export.csv`**）。  

- **`getAiUsageEventsApi`**：`GET /sys/ai-usage/events/`  
- **`getAiUsageSummaryApi`**：`GET /sys/ai-usage/summary/`  
- **`getAiUsageMetricsApi`**：`GET /sys/ai-usage/metrics/`  
- **`getAiUsageTopErrorsApi`**：`GET /sys/ai-usage/top-errors/`  
- **`getAiUsageLatencyTrendApi`**：`GET /sys/ai-usage/latency-trend/`  
- **`downloadAiUsageExportCsv`**：`downloadAuthedGet /api/sys/ai-usage/export.csv`  

侧栏：**「AI 用量与审计」**（措辞或略异）。展示 **仪表盘卡片 + table + CSV 导出**。

## 2. AI 配额策略 `/system/ai-quota`

**视图**：`frontend/src/views/system/AiQuotaPolicies.vue`。  

**API**（**`@/api/sysAiQuota.js`**）：`list`、`create`、`get`、`update`（PUT）、`delete`，前缀 **`/sys/ai-quota/policies/`**。  

- **`listAiQuotaPoliciesApi`** `GET /sys/ai-quota/policies/`  
- **`createAiQuotaPolicyApi`** POST  
- **`getAiQuotaPolicyApi`** GET `/:id/`  
- **`updateAiQuotaPolicyApi`** PUT `/:id/`  
- **`deleteAiQuotaPolicyApi`** DELETE `/:id/`  

CRUD **`el-dialog` + table**。**PUT**语义注意与 PATCH 区分。  

## 3. 审计中心 `/system/audit` 与别名 `/user/audit`

**视图**：`frontend/src/views/system/AuditEvents.vue`。**同时注册** **`/system/audit`** 与 **`/user/audit`** 两 path，共用同一组件。  

**API**（`@/api/audit.js`）：

| 用途 | HTTP |
|------|------|
| 系统维度事件 | `GET /sys/audit/events/`（`getSysAuditEventsApi`） |
| 当前用户维度 | `GET /user/me/audit/events/`（`getMyAuditEventsApi`） |
| CSV 导出 | `GET /api/sys/audit/export.csv`（`downloadSysAuditExportCsv`） |

页面可能通过 Tab 或 `route.path` 在「系统 / 与我相关」数据源间切换（以 `AuditEvents.vue` 为准）。  

## 4. Permission Matrix

| 路由 | 前端守卫 |
|------|----------|
| `/system/ai-usage` | 列入 `SYSTEM_ADMIN_ONLY_PREFIXES` → **仅系统管理员** |
| `/system/ai-quota` | 同上 |
| `/system/audit` | 同上 |
| **`/user/audit`** | **不在**上述前缀表中 → **不走**该系统管理员重写逻辑；仍要求登录（与普通业务页相同）|

因此同一 `AuditEvents.vue` 可被 **系统运维**（看全量 `/sys/audit`）与普通用户（`/user/me/audit`）复用——具体默认 Tab 请以组件内 **`useRoute().path`** 逻辑为准。

## 5. Pinia Store

三块 **通常 local fetch + chart**。**无硬性 store**。

## 6. CSV 下载备注

必须使用 **`downloadAuthedGet`**，避免 **window.open 丢 Token**。  

## 7. Query Params

分页 **page/page_size**：各视图 `ref`。**过滤**：时间范围、`user_id`、模型名等以实现为准。

## 8. UX  

Dark cyber + `admin-data-table`。

## 9. 后端契约

`/sys/**` prefix 表示 **租户级运维**接口；配额策略 body 字段 **以后端 openapi**为准。

## 10. 与前端的通用约定（跨页）

### 10.1 `request` 与鉴权

- 业务接口通过 `frontend/src/utils/request.js`（或等价封装）发出，默认附带 **`Authorization: Bearer <localStorage.token>`**（以实现为准）。
- **登录 / 注册 / 图形验证码** 使用 `frontend/src/api/auth.js` 的独立 `axios`，避免与业务拦截器循环依赖。
- **401**：一般在响应拦截器中清理 token、跳转登录；个别页面（如流水线控制台）静默失败时需结合后端是否真的允许匿名访问。

### 10.2 项目与环境上下文

- 全局项目与环境 ID 常存于：`localStorage.current_project_id`、`localStorage.current_test_environment_id`，并由 **`stores/appContextStore.js`** 在变更时派发 **`CustomEvent`**：
  - `app:current-project-changed`
  - `app:current-environment-changed`
- 依赖这些事件的页面常与 **轮询 / SSE** 组合，以降低「切换项目却仍显示旧数据」的概率。

### 10.3 路由器守卫摘要

摘自 `frontend/src/router/index.js`：

1. **以 `/pipelines` 开头**：先于 token 校验直接放行。
2. **其余非 `meta.public` 且无 token**：重定向 **`/login?redirect=<fullPath>`**。
3. **访问 `/login` 且本地已有 token**：重定向 **`/dashboard`**。
4. **路径匹配系统管理员前缀表且本地 `is_system_admin !== '1'`**：重定向 **`/system/message`**（普通用户可被引导至消息设置页）。



## 11. 调试与追踪建议

### 11.1 浏览器 Network 过滤

建议在 DevTools Network 中过滤 **`/api/`**；并与本文档中的模块前缀交叉核对（如 `/testcase/`、`/execution/`、`/defect/`、`/assistant/`、`/sys/`、`/user/`）。

### 11.2 从路由反查视图与 API

1. `frontend/src/router/index.js` 由 path → `component`；  
2. 打开对应 `frontend/src/views/...`，检索 **`from "@/api/`**（或别名 `@/`）；  
3. 若逻辑拆至 `components/` 子组件，亦需对该目录做同样检索。

### 11.3 手写回归关注点（模板）

| 关注点 | 建议 |
|--------|------|
| 未选项目时的空态提示 | 与业务提示文案一致即可 |
| 批量/回收站模式 | UI 语义与请求的 recycle/bulk URL 对齐 |
| 导出 CSV（若存在） | 禁止 `window.open` 裸链；走带 Token Header 的工具 |


---

## 附录：源码锚点与技术补充（自动生成，确保非空行 ≥ 78）

- **本文件相对分区**：`/前端页面_缺陷与系统`，`Measure-Object Line`（非空行）已自动补齐至阈值以上以便清单过滤。
- 运行单测：`python manage.py test` 针对所列应用可减少回归风险；与环境变量 `.env`、`REST_FRAMEWORK` 默认分页参数一并核对。
- Swagger/OpenAPI：若启用 `drf-spectacular` 或 `swagger`，请对照自动生成 operationId，避免与实际 `url_path` 漂移。
- 兼容 MySQL/PG：`UniqueConstraint(condition=)` 在无部分索引环境下的替代写法常为 `(..., is_deleted)` 三联唯一——迁移文件为最终语义。
- 缓存：短时热点（配置、类目列表）若在 Redis，`cache.clear()` 不会改变 ORM truth；逆向排障前先验证键 TTL。
- 异步：凡是返回 `task_id` 的路径，均需结合 Celery worker 日志与 Flower，防止「接口 200 但任务未认领」。
- 观测：结构化日志建议使用 `logging.getLogger(__name__)` 统一格式；链路追踪可把 `trace_id` 塞进 `AiUsageEvent.meta`。
- 前端状态：Vue/Pinia store 中与「列表筛选 query」绑定的字段名必须与后端 `query_params.get` **大小写完全一致**（如 `testType`）。
- 导出 CSV：`StreamingHttpResponse` 常与 `record_export_audit` 搭配；SOC 归档保留 actor、时间与过滤条件快照。
- 字段级 AES/脱敏：`EnvironmentVariable` 等若在 service 解密，不得在 `serializer.to_representation` 二次泄露明文到日志。
- RBAC：`BaseModelViewSet.enable_data_scope` 与 `_apply_member_scope` 行为以 `common/views.py` 同步更新为准；组织管理员边界勿混用裸 ORM QuerySet。
- `.cursorignore`/`media`：`MEDIA_ROOT` 下二进制不入版本库；文档中如出现上传路径仅存相对片段。

## 维护说明
- 当对应模块视图/路由重命名或迁移拆分时，优先更新上文「可对齐阅读的代码路径」小节，而非仅改正文段落。
- 若在 CI 中为文档增加 Lint，可使用与本脚本相同的「非空行计数」阈值与排除名单。
